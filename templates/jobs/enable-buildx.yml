.enable-buildx:
  script:
    - docker context create build-multiarch || true
    - docker context use build-multiarch || true
    - docker run --rm --privileged multiarch/qemu-user-static --reset -p yes
    - docker buildx create build-multiarch --use || true
    - echo "-----------------ACTION REQUIRED------------------------"
    - echo "WARNING - This template is outdated, everything is already included in the base template, you can safely remove this import. "
    - echo "-----------------ACTION REQUIRED------------------------"