.enable-buildx:
  script: |
    echo "-----------------ACTION REQUIRED------------------------"
    echo "--------------------------------------------------------"
    echo "--------------------------------------------------------"
    echo "This template "enable-buildx.yml" that you are using is outdated, everything is already included in the base template now, you can safely remove this import from your gitlab ci."
    echo "--------------------------------------------------------"
    echo "--------------------------------------------------------"
    echo "-----------------ACTION REQUIRED------------------------"

